# Dashboard access (HTTP Basic Auth via middleware.ts).
# If DASHBOARD_PASSWORD is unset, the dashboard is open (local dev only).
DASHBOARD_USER=ceva
DASHBOARD_PASSWORD=

# Sender name used in generated outreach sequences.
SENDER_NAME=Sukhpreet Singh, CEVA Logistics
