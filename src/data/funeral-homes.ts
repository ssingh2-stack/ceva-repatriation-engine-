import type { FuneralHome } from "@/lib/types";

// ⚠️ PLACEHOLDER — replaced by the real researched GTA list in Task 8.
// Kept minimal so the app compiles while research agents assemble real data.
const FUNERAL_HOMES: FuneralHome[] = [];

export function getFuneralHomes(): FuneralHome[] {
  return FUNERAL_HOMES;
}
